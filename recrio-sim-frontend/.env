# Frontend (Vite) environment variables

# Your simulation backend (Express/Socket.IO) base URL
VITE_API_BASE=http://127.0.0.1:4000

# (Optional) ATS backend, if you’re running it locally too
VITE_ATS_BASE=http://127.0.0.1:4001

# (Optional) defaults used if URL doesn’t provide ?org=&role=
VITE_DEFAULT_ORG=sga
VITE_DEFAULT_ROLE=swe
